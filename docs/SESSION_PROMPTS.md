# Session 使用者指令紀錄（自然語言）

本檔記錄：**本對話從一開始到目前，你輸入過的每一條指令／需求**（自然語言原文）。  
不是終端機指令，也不是只取最初五條。

---

## 時間序總表

| 序 | 你輸入的自然語言（摘要／原文） | 助手做了什麼（極短） |
|----|--------------------------------|----------------------|
| 1 | 五條開發需求（爬蟲、測試、HTML+Mermaid、註解教學、Harness Loop-Verify-Debug） | 建立專案骨架與實作 |
| 2 | 增加 搜尋功能 | 加入關鍵字搜尋 |
| 3 | 推版到 https://github.com/johnGitHub24/python1 | git init／commit／push |
| 4 | 增加資訊檢索功能 | 加入 TF-IDF IR（`--ir`） |
| 5 | 改出問題 退上一次版本 | 還原到推版後、IR 前 |
| 6 | 回上一動 | 恢復 IR 相關改動 |
| 7 | parser／retrieval／fetcher 增加詳細註解 & 程式重構 | 三模組重構＋註解 |
| 8 | 測試驗證 RUN 沒有問題 | 跑 pytest／harness／CLI 驗證 |
| 9 | 單元測試 整合測試寫出網頁報告給我 | 產生 HTML 測試報告 |
| 10 | SHA: e9d5bf5… 退回這一版本 | `git reset --hard` 到該 commit |
| 11 | 寫給初學者看得程式語法使用註解 | 核心模組加【語法】註解 |
| 12 | 每次做完任務 要同步持續更新相關文件 | 建立 Cursor 規則＋同步文件 |
| 13 | 把所有開發過的使用指令 整理文件md檔案 | 寫 `docs/COMMANDS.md` |
| 14 | 彙整我整理送出下指令的語法 顯示出來 | 顯示 COMMANDS 語法一覽 |
| 15 | 自然語言的語法 | 先做成操作人話對照（誤解） |
| 16 | （澄清）我說是我輸入的指令！＋貼回最初五條 | 改做成需求語法檔（仍偏最初五條） |
| 17 | 但不是這個五個 我是說這個 session 從一開始到目前的紀錄 | **本檔：全程指令紀錄** |
| 18 | 增加 並推版 | 提交現有變更並推送到 GitHub |

---

## 逐條原文

### 01｜專案啟動（五條需求）

```text
1. 撰寫python 爬蟲 新聞資訊程式

2. 使用單元測試與整合測試開發

3. 轉寫html 指導教學 & 畫流程圖 mermaid

4. 撰寫程式註解教學 

5. harmess&loop engineering 
loop - verify - debug 
```

### 02｜功能

```text
增加 搜尋功能  ~
```

### 03｜發布

```text
推版 到https://github.com/johnGitHub24/python1 
```

### 04｜功能

```text
增加資訊檢索功能
```

### 05｜版本回退

```text
改出問題  退上一次版本
```

### 06｜撤銷回退

```text
回上一動
```

### 07｜重構／註解

```text
@src/news_scraper/parser.py @src/news_scraper/retrieval.py @src/news_scraper/fetcher.py
增加詳細註解 & 程式重構
```

### 08｜驗證

```text
測試驗證 RUN 沒有問題 
```

### 09｜報告

```text
單元測試 整合測試寫出網頁報告給我
```

### 10｜指定版本回退

```text
SHA: e9d5bf5abab73fbc2a8ace8e6f151c8284b78291  退回這一版本
```

### 11｜教學註解

```text
寫給初學者看得程式語法使用註解
```

### 12｜流程規範

```text
每次做完任務 要同步持續更新相關文件 
```

### 13｜文件整理

```text
把所有開發過的使用指令 整理文件md檔案
```

### 14｜顯示指令語法

```text
彙整我整理送出下指令的語法 顯示出來 
```

### 15｜自然語言語法（當時語意待澄清）

```text
自然語言的語法
```

### 16｜澄清：是「我輸入的指令」

```text
1. 撰寫python 爬蟲 新聞資訊程式
…（五條）…
我說是我輸入的指令! 
```

### 17｜再澄清：要整個 session 紀錄

```text
但不是這個五個  我是說這個session 從一開始到目前的紀錄
```

### 18｜提交並推版

```text
增加 並推版 
```

---

## 文件分工（避免再搞混）

| 檔案 | 內容 |
|------|------|
| **本檔 `SESSION_PROMPTS.md`** | 本 session 你下過的**全部自然語言指令** |
| `REQUIREMENTS_NL.md` | 最初五條需求的語意拆解 |
| `COMMANDS.md` | 終端機 `py -3`／`pytest`／`git` 指令 |
| `NATURAL_LANGUAGE.md` | 「想做某事」→ 對應終端機指令 |

---

## 維護

之後在本對話若再下新指令，應把原文追加到本檔「逐條原文」與「時間序總表」。
