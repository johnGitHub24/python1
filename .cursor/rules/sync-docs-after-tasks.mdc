---
description: 完成任務後必須同步更新相關文件（README、教學、結構說明）
alwaysApply: true
---

# 任務完成後同步更新文件

每次功能開發、重構、測試調整或行為變更結束時，**不可只改程式碼就收工**。必須檢查並更新受影響的文件，保持文件與程式一致。

## 必查清單（依變更內容勾選）

| 變更類型 | 要更新的文件 |
|----------|----------------|
| 新功能 / CLI 參數 / 用法改變 | `README.md`、`docs/tutorial.html`、`docs/COMMANDS.md`、`docs/NATURAL_LANGUAGE.md` |
| 架構、資料流、模組增刪 | `docs/tutorial.html`（含 Mermaid）、`README.md` 專案結構 |
| 測試方式、報告、標記改變 | `README.md` 測試章節、`docs/COMMANDS.md`、`pytest.ini`（若有） |
| 依賴套件增刪 | `requirements.txt`、`README.md` 安裝說明、`docs/COMMANDS.md` |
| 公開 API / 匯出改變 | `src/news_scraper/__init__.py`、教學中的模組說明 |
| 僅註解／初學者說明強化 | 若教學頁有「註解教學」章節，一併對齊用語 |

## 執行原則

1. **同一輪任務內完成**：文件更新與程式變更一起交付，不要留「之後再補」。
2. **只改有差的地方**：不要為了同步而大改無關章節。
3. **範例指令要可跑**：README / tutorial 裡的 `py -3 ...` 指令必須對應當前 CLI。
4. **結構樹要真實**：`README` 專案結構列出的路徑必須存在。
5. **收工前自問**：若新人只看文件，能否做出與程式相同的操作？

## 不要做的事

- 不要新增未經要求的長篇 markdown 文件（除非使用者要求或上表已列既有文件）。
- 不要把暫時實驗、未合併行為寫進正式教學當已完成功能。
